{
  "name": "encurtador-api",
  "version": "1.0.0",
  "description": "API REST de encurtamento de URLs - projeto da disciplina de DevOps",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "oxlint src tests",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "oxlint": "^1.82.0",
    "supertest": "^7.2.2",
    "vitest": "^5.0.0"
  }
}
