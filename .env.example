# Porta em que a API escuta
PORT=3000

# URL base usada para montar o link curto devolvido pela API
# (nome proposital: BASE_URL colide com uma variavel reservada do Vite)
URL_BASE=http://localhost:3000

# Tamanho do codigo curto gerado (entre 4 e 12)
TAMANHO_CODIGO=7
